import { Audiowide, Orbit } from "next/font/google";

export const audiowide = Audiowide({
  subsets: ["latin"],
  weight: ["400"],
  display: "swap",
});

export const orbit = Orbit({
  subsets: ["latin"],
  weight: ["400"],
  display: "swap",
});
